<div class = space></div>
 <%= form_tag("/tweets/#{@tweet.id}/comments/#{@comment.id}/edit", method: :patch ) do %>
 <div class = las>
    <h3>
      編集する
    </h3>
    <textarea name="text" placeholder="text" rows="10" cols="30"><%= @comments.text %></textarea>
    <input type="submit" value="SEND">
    </div>
  <% end %>